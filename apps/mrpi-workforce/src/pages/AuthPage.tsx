import { Eye,EyeOff,HardHat,LockKeyhole,Mail,ShieldCheck } from 'lucide-react'
import { useEffect,useState,type FormEvent } from 'react'
import { useLocation } from 'wouter'
import { useAuth } from '../auth/AuthProvider'
import { Brand } from '../components/Brand'
import { supabase } from '../lib/supabase'

export function AuthPage(){const [reset,setReset]=useState(false);const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [show,setShow]=useState(false);const [busy,setBusy]=useState(false);const [error,setError]=useState('');const [notice,setNotice]=useState('');const {session}=useAuth();const [,navigate]=useLocation();useEffect(()=>{if(session)navigate('/',{replace:true})},[navigate,session]);async function submit(event:FormEvent){event.preventDefault();if(!supabase)return;setBusy(true);setError('');setNotice('');try{if(reset){const {error:e}=await supabase.auth.resetPasswordForEmail(email.trim(),{redirectTo:`${window.location.origin}/tetapan-kata-laluan`});if(e)throw e;setNotice('Pautan tetapan semula telah dihantar ke e-mel kau.')}else{const {error:e}=await supabase.auth.signInWithPassword({email:email.trim(),password});if(e)throw e;navigate('/',{replace:true})}}catch(caught){const raw=caught instanceof Error?caught.message:'Log masuk tidak berjaya.';setError(raw.toLowerCase().includes('invalid login')?'E-mel atau kata laluan tidak tepat.':raw)}finally{setBusy(false)}}return <main className="min-h-screen lg:grid lg:grid-cols-2"><section className="hidden bg-slate-950 p-12 text-white lg:flex lg:flex-col lg:justify-between"><Brand/><div className="max-w-xl"><div className="mb-6 grid h-16 w-16 place-items-center rounded-2xl bg-sky-400 text-slate-950"><HardHat className="h-8 w-8"/></div><p className="text-sm font-black uppercase tracking-[.2em] text-sky-300">Attendance tapak</p><h1 className="mt-4 text-5xl font-black leading-tight">Nama, kehadiran dan upah. Itu sahaja.</h1><p className="mt-5 text-lg leading-8 text-slate-300">Admin menandakan attendance menggunakan akaun MRPI yang sama.</p></div><p className="flex items-center gap-3 text-sm text-slate-400"><ShieldCheck className="h-5 w-5 text-sky-400"/>Pekerja tidak memerlukan akaun atau maklumat peribadi.</p></section><section className="flex min-h-screen items-center justify-center px-5 py-8"><div className="w-full max-w-md"><div className="mb-8 lg:hidden"><Brand/></div><div className="card p-6 sm:p-8"><p className="eyebrow">Satu akaun MRPI</p><h2 className="mt-2 text-2xl font-black">{reset?'Tetapkan semula':'Log masuk Workforce'}</h2><p className="mt-2 text-sm text-slate-600">Gunakan akaun Contractor Suite yang sama.</p><form onSubmit={submit} className="mt-6 space-y-4"><label><span className="field-label">E-mel</span><span className="relative block"><Mail className="field-icon"/><input className="field-control pl-11" type="email" required autoComplete="email" value={email} onChange={(e)=>setEmail(e.target.value)}/></span></label>{!reset&&<label><span className="field-label">Kata laluan</span><span className="relative block"><LockKeyhole className="field-icon"/><input className="field-control px-11" type={show?'text':'password'} required minLength={8} autoComplete="current-password" value={password} onChange={(e)=>setPassword(e.target.value)}/><button type="button" onClick={()=>setShow((v)=>!v)} className="absolute right-1 top-1 grid h-10 w-10 place-items-center rounded-lg text-slate-500">{show?<EyeOff className="h-5 w-5"/>:<Eye className="h-5 w-5"/>}</button></span></label>}{error&&<p className="alert-error">{error}</p>}{notice&&<p className="alert-success">{notice}</p>}<button className="btn-primary w-full" disabled={busy}>{busy?'Sila tunggu...':reset?'Hantar pautan':'Log masuk'}</button></form><button type="button" onClick={()=>{setReset((v)=>!v);setError('');setNotice('')}} className="mt-4 min-h-11 w-full rounded-xl text-sm font-bold text-slate-600 hover:bg-slate-50">{reset?'Kembali ke log masuk':'Lupa kata laluan?'}</button></div></div></section></main>}
